fun sayHi(first, last) {
  print "Hi, " + first + " " + last + "!";

  var i = 1;
  while(i<=3) {
      if(i==3) {
        print i;
        return i;
      }

      i=i+1;
  }

  return "something";
}

fun sayHi1(first, last) {
  print "Hi, " + first + " " + last + "!!";
}

var res = sayHi("Dear", "Reader");
print res;
